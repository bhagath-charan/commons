dependencies {
    api project(":commons-api")

    annotationProcessor ("org.mapstruct:mapstruct-processor:${MAPSTRUCT_VERSION}")

    implementation ("org.springframework.data:spring-data-mongodb:${SPRING_DATA_VERSION}")
}